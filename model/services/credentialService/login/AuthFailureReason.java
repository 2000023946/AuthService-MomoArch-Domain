package com.authservice.domain.model.services.credentialService.login;

/**
 * Categorized reasons for authentication failure.
 */
public enum AuthFailureReason {
    INVALID_CREDENTIALS,
    ACCOUNT_LOCKED
}